#include<bits/stdc++.h>
using namespace std;
#define MAX 1000;

bool hash[MAX+1][2];

bool insert(int a[],int n){
for(int i=0;i<n;i++){
    if(a[i]>=0)
        hash[a[i]][0]=true;
    else 
        hash
}
}